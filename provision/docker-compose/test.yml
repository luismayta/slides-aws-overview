# Slides-aws-overview: Docker Compose
# https://docs.docker.com/compose/
#
# Usage: test up
version: '3'
services:

  app:
    build:
      context: .
      dockerfile: provision/docker/app-test/Dockerfile
